import { BsJoystick } from "react-icons/bs";
import styled from "styled-components";

export const LogoForContentBox = styled(BsJoystick)`
  transform: scale(2);
  color: ${({ theme }) => theme.colors.whiteColor};
`;
